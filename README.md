# CommSkill Pro 🎙️
**AI-Powered Communication Skills Platform**

## Modules
- 🎤 Live AI Voice Assistant
- 👥 Group Discussion Simulator  
- ⏱️ JAM Session (Just A Minute)
- ✍️ Grammar Correction

## Tech Stack
- Frontend: React 19 + Vite
- Backend: Node.js + Express + Socket.io
- AI: Groq SDK + Whisper STT

## Setup
```bash
# Frontend
cd frontend && npm install && npm run dev

# Backend
cd backend && npm install && node server.js
```
```

---

## ⚠️ Important — Protect Your API Keys!

Make sure your `.env` file is in `.gitignore` and **never push it**. Create a `.env.example` instead:
```
GROQ_API_KEY=your_key_here
PORT=5000